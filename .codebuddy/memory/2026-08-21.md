# 2026-08-21

## 打包配置
- pom.xml `<build>` 添加 `<finalName>snapshot</finalName>`，打包产物固定为 snapshot.war
- **重要：Maven 环境 JAVA_HOME 指向 E:\jdk-11.0.15（JDK11），但 pom 要求 source/target=17，直接 mvn 会报"无效的目标发行版：17"**
- **正确打包命令：`set "JAVA_HOME=C:\Program Files\Java\latest\jdk-17" && mvn -q clean package -DskipTests`**（JDK17 在 C:\Program Files\Java\latest\jdk-17）

## ooeluc_t 产线/成本中心接口修复
- **ooeluc_t 表列名规则：ooeluc + 后缀，账套=ooelucent、据点=ooelucsite（不是 ooleucent/ooleucsite！）**
- 前端参数名 ooleucent/ooelucent 均可（Controller 兼容读取），params 一律用数据库列名 ooelucent
- OoelucSqlProvider + IndexController 已修正，snapshot.war 已验证无错误拼写

## ooeluc_t 真实表结构（2026-08-21 用户提供表结构图确认）
- 主键：ooelucent(企业代码) + ooelucsite(营运据点) + ooeluc001(部门) + ooeluc002(产线)
- ooeluc003 = 产线名称
- ooeluc004 = **备注**（表结构中文名，用户仍按需求当作"成本中心编码"返回）
- 表里没有独立的"成本中心编码"字段
- 接口 /queryOoeluc 最终按用户确认：返回 ooeluc003 + ooeluc004
- 2026-08-21 追加：OoelucSqlProvider 加 ORDER BY ooeluc001, ooeluc004, ooeluc003
