## 2025-08-11

### 新增预算与实际差异明细表功能
- 创建 GlapMapper + GlapSqlProvider（primary DS）: 查询 glaq_t/glaq_t/glacl_t/oofel_t 实际费用，Oracle DECODE 行转列按月汇总
- 创建 BudgetActualSqlProvider（second DS）: 查询 bgbsuc_t 预算，Oracle DECODE 行转列按月汇总
- 扩展 DsdataMapper: 新增 queryBudgetSummaryMonthly 方法
- IndexController 新增 /queryBudgetActualVariance 端点: 跨数据源 LEFT JOIN（实际 LEFT JOIN 预算），Java 层计算12个月差异和合计
- 前端入参: ent(账套), site(账别), year(年度), dept(部门), subjectName(科目名称), summary(摘要)
