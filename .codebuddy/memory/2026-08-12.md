## 2026-08-12

### 新增合计差异字段
- 需求：返回字段加"合计差异" = 实际合计 - 预算合计
- 修改 BudgetActualSqlProvider：外层 SELECT 增加 `合计差异金额` 列（实际合计表达式 - 预算合计表达式）
- 修改 IndexController：`合计差异金额` → `合计差异`
- 注意：字段名用户明确要"合计差异"（不是"差异合计"）

### 部门条件排查
- 用户反馈前端填了部门但过滤没生效
- 已确认后端 SQL 有 `AND ooefl003 LIKE '%dept%'`，Controller 也接收 dept 参数
- 在 IndexController 增加参数日志 System.out.println("[queryBudgetActualVariance] params: ...") 便于排查前端传参
