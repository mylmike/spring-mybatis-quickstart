# 2026-08-18

- MainLayout.vue 菜单调整:将「计划排程」从普通菜单项改为 `el-sub-menu`,其下新增子菜单「主计划排程」(index: main-schedule)和「车间计划排程」(index: shop-schedule);`el-menu` 设置 `:default-openeds="['schedule', 'finance']"` 让子菜单默认展开。
- 页面拆分:旧 `ProductionSchedule.vue` 已删除,拆分为两个组件:
  - `MainSchedule.vue`(主计划排程):表单(企业代码/单号/成本中心/订单号/订单序号/预计日期/营运据点/品号)+ BOM 结构树形展示(el-table 树形,完整显示所有层级,列:序、BOM行[顶层 level=1 显示主件品号 bmba001,其余层级显示元件品号 bmba003;后接项次/单位]、BOM用量、实际用量、订单需求用量、层级;支持展开/收起)。
  - `ShopSchedule.vue`(车间计划排程):原左右排程页面(左侧待排程工单→右侧排程、保存/删除)。
- API `fetchBomTree`(POST /snapshot/queryOrderBom,参数 token/ent/site/xmdddocno 订单号/xmdd001 品号/xmddseq 订单序号,返回 { tree:[...] });`MainSchedule.vue` 查询时传 xmddseq=form.orderSeq(单头"订单序号"字段)。
- MainSchedule.vue 表格列调整(用户明确要求):「订单号」「订单序号」绑定返回数据字段(prop="订单号"/"订单序号")独立两列显示、不参与树形、与 BOM 行不拼接;「BOM 行」列移到第一列作为树形目录列(带展开箭头/缩进),并移除了原来的「序」index 列。树形层级始终按 BOM 结构(level=1 主件 bmba001,其余 bmba003)。
