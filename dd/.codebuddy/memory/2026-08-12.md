# 2026-08-12

## 预算报表功能（预算报表）
- 新增 `src/components/BudgetReport.vue`：单头查询条件（账套 ent/账别 site/年度 year/科目名称 subjectName/摘要 summary）+ 单身明细表（部门、科目、科目名称、预算合计、实际合计、每月 本年/预算/差异 三列，1~12 月嵌套表头，差异负数标红），支持导出 Excel（xlsx）。
- 在 `src/api/budget.js` 新增 `queryBudgetReport`，POST `/snapshot/queryBudgetActualVariance`（用户提供真实地址 `http://192.168.0.85:85/snapshot/queryBudgetActualVariance`），token 固定 `e6338a4acxw502kmf5dwr316ss8u0ymb`。
- 在 `src/components/MainLayout.vue` 注册组件并替换 `budget-report` 菜单占位。
- 后端返回的月度预算值藏在对象 `NVL(YS` 下的键 `预算0X月,0)`（SQL 别名 `NVL(YS.预算0X月,0)` 的产物），组件内 `getBudget()` 已做兼容提取。
- 后端月度字段名已确认为 `实际0X月`（原先误用 `本年0X月`），`BudgetReport.vue` 中表格、导出 Excel 均已改为 `实际0X月`，列头显示"实际/预算/差异"。
- 表格左侧冻结列包括：序号、部门、科目、科目名称、预算合计、实际合计、合计差异（后端字段 `合计差异`，位于 `实际合计` 之后，负数标红，导出 Excel 也包含）。
- 页面布局：明细卡片（detail-card）不再显示标题行，表格直接顶满卡片内容区；查询条件卡片顶部也不显示标题。外层 gap 由 12px 压至 4px，detail-card body 顶部 padding 由 12px 压至 4px，进一步消除表格前空隙。
- 单头查询条件新增"部门"输入框，请求 payload 新增 `dept`（部门名称，模糊匹配，可选），位于 `year` 之后、`subjectName` 之前。
- 账套、账别、年度为必填项：模板上均带 required 标记，`handleQuery` 中逐项校验，缺失时 ElMessage.warning 提示。
- 单头表单已压缩：默认 label-width 56px，字体 12px；"科目名称"项单独 84px；账套 60px / 账别 80px / 年度 100px / 部门 120px / 科目名称 150px / 摘要 120px，表单项间距 6px/8px。查询/重置/导出Excel 三个按钮通过 `.btn-group` 居中显示。
- 注意：`src/api/budget.js` 的 `queryBudgetReport` 请求体字段需与组件 payload 保持一致。曾因漏传 `dept` 导致部门过滤不生效，已修复（补上 `dept: params.dept || ''`）。

## 项目约定
- 后端 baseURL 见 src/api/ 下各文件；所有请求都带固定 token。
- 组件放 src/components，菜单注册在 MainLayout.vue 的 componentsMap。
